
const initialState = {
    email: '',
    password: '',
    rememberMe: false,
};

const loginReducer = (state = initialState, action) => {
    switch (action.type) {
        case '':
            return {

            }
    }
};


export default loginReducer;
